from tkinter import *

root = Tk()

def generate_button_click():
    path_label = Label(root, )

generate_button = Button(root, text="Generate Protocol")
generate_button.grid(row=0, column=0)

root.mainloop()

